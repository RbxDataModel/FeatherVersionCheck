local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

while true do
    -- Function to kick the local player
    local function kickLocalPlayer()
        if localPlayer then
            localPlayer:Kick("stop skidding")
        end
    end
end
